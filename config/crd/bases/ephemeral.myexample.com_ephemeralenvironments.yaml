---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: ephemeralenvironments.ephemeral.myexample.com
spec:
  group: ephemeral.myexample.com
  names:
    kind: EphemeralEnvironment
    listKind: EphemeralEnvironmentList
    plural: ephemeralenvironments
    singular: ephemeralenvironment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.expiryTime
      name: ExpiryTime
      type: date
    - jsonPath: .status.reason
      name: Reason
      type: string
    - jsonPath: .spec.targetNamespace
      name: TargetNS
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EphemeralEnvironment is the Schema for the ephemeralenvironments
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of EphemeralEnvironment
            properties:
              action:
                enum:
                - ScaleToZero
                - Delete
                type: string
              targetNamespace:
                type: string
              ttl:
                type: string
            required:
            - action
            - targetNamespace
            - ttl
            type: object
          status:
            description: status defines the observed state of EphemeralEnvironment
            properties:
              expiryTime:
                format: date-time
                type: string
              reason:
                type: string
              state:
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
